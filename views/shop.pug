<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{docTitle}
        link(rel="stylesheet", href="/css/main.css")
    body
        header.main-header
            nav.main-header__nav
                ul.main-header__item-list
                    li.main-header__item
                        a(class="active", href="/") Shop 
                    li.main-header__item
                        a(class="", href="/admin/add-product") Add Product 
        main
            h1 My Products
            p List of all the products...via Pug template engine
            div(class="grid")
                each product in prods
                    article(class="card product-item")
                        header(class="card__header")
                            h1(class="product__title") #{product.title}
                        div(class="card__image")
                            image(src="https://www.adazing.com/select/colors/preview-06.jpg", alt="Book" width="200", height="200")
                        div(class="card_content")
                            h2.product__price $19.95
                            p.product_description A Very interesting book so many even more
                        .card_actions
                            button.btn Add to Cart



